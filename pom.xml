<project>
    <!-- A unique identifier for the project's group. -->
    <groupId></groupId>
    <!-- A unique identifier for the project's artifact (e.g., the JAR file). -->
    <artifactId></artifactId>
    <!-- The version number of the project. -->
    <version></version>

    <build>
        <!-- The directory containing your Java source files. -->
        <sourceDirectory>src/main/java</sourceDirectory>
        <!-- The directory where compiled classes will be placed. -->
        <outputDirectory>target/classes</outputDirectory>
    </build>

    <dependencies>
        <!-- Add dependencies here if needed -->
    </dependencies>
</project>
